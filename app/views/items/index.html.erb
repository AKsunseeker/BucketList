<%= render(partial: 'layouts/navbar') %>
<div class="container">
  <div class="center-align">
    <h2>Items</h2>
    <% @items.each do |item| %>
      <%= link_to item.name, list_item_path(@lit, item) %>
    <% end %>
  </div
</div>